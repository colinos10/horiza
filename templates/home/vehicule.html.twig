{% extends 'base.html.twig' %}

{% block title %}véhicule{% endblock %}

{% block body %}

<<<<<<< HEAD
    <div class="container-fluid p-5 text-center bg-light">
        <h1>HORIZA - {{ vehicule.marque }}</h1>
    </div>
=======
    <header class="container-fluid p-5 text-center bg-light">
        <h1>{{ vehicule.marque }} - {{ vehicule.modele }}</h1>
    </header>
>>>>>>> 7d51b98251625c550e89f5e44ae8facb24bd7f7d

    <main class="container-fluid">
        <div class="row p-5">
            <div class="col-8 offset-2">
                <img src="{{ asset('img/vehicule/' ~ vehicule.img1) }}" alt="photo principale {{ vehicule.marque }}" class="w-100">
                <h2 class="py-3">{{ vehicule.marque }}</h2>
                <p>{{ vehicule.modele }}</p>
                <ul>
                    <li>Nombre de places : {{ vehicule.nombrePlace }}</li>
                    <li>Immatriculation : {{ vehicule.immatriculation }}</li>
                    <li>Couleur : {{ vehicule.couleur }} </li>
                    <li>carburant : {{ vehicule.carburant }}</li>
                    <li>Disponibilité : {{ vehicule.disponibilite }}</li>
                    <li>Type de véhicule : {{ vehicule.typeVehicule }}</li>
                    <li>Prix : {{ vehicule.prix|number_format(0, ',', ' ')}} €</li>
                </ul>
            </div>
            {% if vehicule.img2 is not null %}
                <div class="col-8 offset-2">
                    <h2 class="py-3">Autres photos</h2>
                    <img src="{{ asset('img/vehicule/' ~ vehicule.img2) }}" alt="photo principale {{ vehicule.marque }}" class="w-50">
                </div>
            {% endif %}
        </div>

        <div>
            <p>CONTACTER LE PROPRIETAIRE</p>
            <p>Nom : </p>
            <p>Numéro de téléphone : </p>
            <p>Email : </p>
        </div>

        <div class="text-right">
            <a href="{{ path('home') }}" type="button" class="btn btn-info">Retour à la page d'accueil</a>
        </div>
    </main>

{% endblock %}